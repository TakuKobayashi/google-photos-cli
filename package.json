{
  "name": "google-photos-cli",
  "version": "0.0.1",
  "description": "This is a CLI tool to work with Google Photos",
  "main": "dist/index.js",
  "author": "TakuKobayashi",
  "license": "MIT",
  "bin": {
    "google-photos-cli": "./bin/google-photos-cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w",
    "format": "prettier --config .prettierrc --write \"src/**/*.{ts,js}\"",
    "docs": "typedoc --out docs/ && touch docs/.nojekyll",
    "jest": "jest -c jest.config.js",
    "ts-node": "ts-node"
  },
  "dependencies": {
    "chalk": "^5.0.1",
    "commander": "^9.2.0",
    "typescript": "^4.6.4"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^18.7.14",
    "documentation": "^14.0.0",
    "dotenv": "^16.0.0",
    "jest": "^29.0.1",
    "prettier": "^2.6.2",
    "ts-node": "^10.7.0",
    "typedoc": "^0.23.13",
    "typings": "^2.1.1"
  }
}
